# Activate with: mvn spring-boot:run -Dspring-boot.run.profiles=keycloak
# Requires Keycloak at http://localhost:8081 (docker compose up -d keycloak)
app.keycloak.enabled=true
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8081/realms/myfundraiser
